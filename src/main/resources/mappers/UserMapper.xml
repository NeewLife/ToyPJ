<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.project.Board.domain.user.UserMapper">
    <sql id="userColumns">
            user_id
        ,   name
        ,   sex
        ,   age_group
        ,   login_id
        ,   password
        ,   nickname
        ,   height
        ,   weight
    </sql>

    <insert id="joinUser">
        INSERT INTO user
            ( name, sex, age_group, login_id, password, nickname, height, weight)
        VALUES
            (#{name},
    </insert>

</mapper>